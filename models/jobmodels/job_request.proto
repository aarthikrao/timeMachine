syntax = "proto3";
package models;

option go_package = "../jobmodels";

enum OperationType {
    GET = 0;
    INSERT = 1;
    REMOVE = 2;
}

message JobRequest {
    string ID = 1;
    int64 TriggerTime = 2; 
    bytes Meta = 3;
    string Route = 4;
    OperationType Operation = 5;
}